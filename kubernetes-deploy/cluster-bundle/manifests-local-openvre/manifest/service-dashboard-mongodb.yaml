apiVersion: v1
kind: Service
metadata:
  name: dashboard-mongodb
  labels:
    app.kubernetes.io/part-of: bsc-tre
spec:
  ports:
  - name: api
    port: 27017
    targetPort: 27017
    protocol: TCP
  selector:
    app: dashboard-mongodb
