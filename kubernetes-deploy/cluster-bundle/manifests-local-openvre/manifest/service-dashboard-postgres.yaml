apiVersion: v1
kind: Service
metadata:
  name: dashboard-postgres
  labels:
    app.kubernetes.io/part-of: bsc-tre
spec:
  ports:
  - name: api
    port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: dashboard-postgres
