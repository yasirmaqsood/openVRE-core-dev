apiVersion: v1
kind: Service
metadata:
  name: scheduler
  labels:
    app.kubernetes.io/part-of: bsc-tre
spec:
  selector:
    app: scheduler
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
